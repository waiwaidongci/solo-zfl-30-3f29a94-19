# 交付说明 —— 出水文物转运装载规划台

日期：2026-09-15 · 基线：`index.html`（水下考古潜水记录，零依赖离线单页应用）

## 1. 交付内容

在现有离线记录页基础上新增「出水文物转运装载规划台」（`planner.html`），与潜水记录页互相链接，同样零运行时依赖、纯 localStorage 离线可用。

| 文件 | 层 | 说明 |
|---|---|---|
| `src/planner-core.js` | 数据层 | 箱/舱模型、六类约束校验、确定性规划器、事务式提交、交接明细。UMD 封装，浏览器与 Node 通用，不依赖 DOM |
| `src/planner-store.js` | 数据层 | localStorage 持久化、种子数据；存储适配器可注入（测试用内存适配器） |
| `src/planner-ui.js` + `planner.html` | 界面层 | 渲染与交互，不含约束逻辑；桌面/手机响应式 |
| `tests/planner-core.test.js` | 测试 | 17 项自动化测试（node:test） |
| `scripts/browser-verify.mjs` | 验证 | 真实浏览器验证脚本（Playwright + Chromium） |
| `index.html` | 修改 | 页头增加规划台入口链接 |

## 2. 需求对照

**登记数据**
- 转运箱：箱号、外廓（长×宽×高）、重量、可承重、重心高度、允许朝向（正放/侧放）、脆弱等级（1–5，≥4 禁止上堆）、类别、禁邻类别、装卸港序。
- 货舱：隔层（deck）、舱位（slot，含行/列/堆叠上限/舱位承重）、全舱重心偏移阈值；箱以港序表达靠港装卸次序。

**约束规则（校验器 `validatePlan`，冲突逐条给出箱号 + 约束名 + 说明）**
1. 承重链：每箱上方总重 ≤ 其可承重；舱位总重 ≤ 舱位承重。
2. 堆叠上限：每舱位叠放箱数 ≤ 上限。
3. 重心偏移：每叠合成重心高（按朝向折算有效高与重心）≤ 阈值 × 叠放总高。
4. 固定方向：放置朝向必须属于箱登记的允许朝向。
5. 禁邻：同一隔层内行列相邻舱位、同一高度层上的两箱，类别不得互在对方禁邻列表。
6. 先卸后装：同一叠内下层箱港序必须 ≥ 上层箱港序（先卸的箱不被压）。
   另：脆弱等级 ≥4 的箱禁止任何上堆。

**行为**
- 箱体或舱位变更后自动重算；重算/提交失败时，已提交的装载图、约束结果、交接明细全部不变（事务式 `commitPlan`：失败返回原状态引用，测试断言深度相等）。
- 冲突时界面列出「箱号 · 约束 · 详情」，确认保存按钮禁用，拒绝保存。
- 规划器确定性：装箱顺序（港序→重量→箱号）、舱位顺序（隔层→行→列）、朝向顺序均为显式排序，无随机/时间源；同一输入重复生成逐字节一致，输入乱序结果一致（有测试）。
- 桌面与手机均可查看、调整（点箱再点舱位移动）、确认；手机单列布局、触屏验证通过。

## 3. 自动化测试结果

`npm test`（入口 `tests/run-tests.js`，直接 `node` 运行、不依赖 `node --test` 的目录扫描，Node 18/20/22 均可直接跑完；失败时退出码非零）：**22/22 通过**。

覆盖：六类约束各自检出且报对箱号、脆弱禁堆、确定性（重复生成/乱序输入）、生成方案自校验通过、容量不足报真实约束（堆叠上限）、提交失败时原装载图/约束结果/交接明细不变、提交成功不改原状态、交接明细港序分组与重量合计、存储往返、登记数据校验。

回归测试（`tests/planner-regression.test.js`，对应 2026-09-15 修复）：
- 超限箱重算失败时报告真实约束（`slot-weight`，含箱号与数值），不得只报笼统 `unplaced`；
- 失败诊断确定性（输入乱序冲突列表一致）、可同时含多种真实约束（承重链 + 舱位承重）；
- 提交失败后已提交的装载图/约束结果/交接明细逐字节不变；已提交方案自身始终通过校验。

## 4. 真实浏览器验证结果

环境：Chromium 153.0.8010.12（Playwright 1.49，headless），本地静态服务器，桌面 1280×800 与手机 390×844（iPhone UA + 触屏）两个视口。**31/31 通过**，控制台无未捕获异常。结果存于 `verify-results.json`，可用 `npm run verify:browser` 复现。

桌面流程（21 项）：首次渲染已提交装载图（6 箱）与状态徽标；交接明细按 3 个港序分组；自动生成→预览→确认保存→刷新后持久化；重复生成布局一致（确定性）；手动移箱→保存→交接明细更新；把 BX-101 重量改为 5000kg 触发重算失败——冲突面板在保留「已提交方案约束结果」的同时列出真实失败约束（BX-101 · 舱位承重/重心偏移/承重链），装载图布局、箱重与舱位现重保持已提交值（不出现草稿值 5000），交接明细不变，状态未保存、确认按钮禁用；放弃草稿恢复；合法变更保存并刷新持久化；舱位堆叠上限调整触发重算；index.html ↔ planner.html 入口互链。

手机流程（9 项）：单列布局、装载图与交接明细可见、触屏生成/确认/刷新持久化、触屏移箱；失败重算时冲突为真实约束（舱位承重）、装载图与交接明细保持已提交值、放弃草稿恢复。

## 5. 复现方式

```bash
npm test                # 22 项数据层测试（node tests/run-tests.js，Node 18/20/22 通用）
npm install             # 仅验证用 devDependency（playwright）
npx playwright install chromium
npm run verify:browser  # 31 项真实浏览器验证，输出 verify-results.json
```

## 6. 修复记录（2026-09-15）

1. **重算失败后的状态混用**：装载图此前用「已提交的摆放 + 草稿的箱/舱数据」渲染，失败时箱重与舱位现重会先显示草稿值。现改为单一数据源视图模型：有预览用草稿三元组（摆放+箱+舱），否则整体用已提交三元组；冲突面板常驻展示已提交方案的约束结果，失败信息单独追加。
2. **冲突说明只报笼统「无法安置」**：规划器装箱失败时现在会对每个舱位×朝向重放候选并收集 `validatePlan` 的真实冲突（按 箱号+约束 去重、确定性顺序），如「BX-101 · 舱位承重：舱位 D1-R1C1 总重 5610kg，超过舱位承重 900kg」；仅在无任何约束可循时兜底报 unplaced。
3. **测试入口兼容性**：`npm test` 由 `node --test tests/`（目录扫描行为随 Node 版本变化）改为 `node tests/run-tests.js` 聚合入口，常见 Node 版本直接跑完，失败退出码非零。

## 7. 假设与边界

- 朝向仅正放/侧放两种；侧放时有效高取箱宽、重心取宽的一半（数据层注释已写明）。
- 禁邻按「同隔层、行列相邻、同高度层」判定；舱位 id 约定 `隔层-行R列C`（如 `D1-R1C1`），用于推导相邻关系。
- 先卸后装按同一叠内港序判定；跨舱位不互相阻挡（舱位均可独立直达）。
- 重心偏移按每叠合成重心高/总高 ≤ 阈值校验，阈值全舱统一、可在舱位设置中调整。
